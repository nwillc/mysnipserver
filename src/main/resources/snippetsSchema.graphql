schema {
    query: Query
}

type Query {
    categories : [Category]!
    category(key: ID!) : Category
}

interface Entity {
    key: ID!
}

type Category implements Entity {
    key: ID!
    name: String!
}
