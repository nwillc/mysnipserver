language: java
jdk:
  - oraclejdk8
branches:
  only:
    - master
env:
  - TERM=dumb
before_script:
  - chmod +x gradlew deploy.sh
script:
  - ./gradlew oneJar --no-daemon
after_success:
  - ./deploy.sh
  - ./gradlew jacocoTestReport
  - bash <(curl -s https://codecov.io/bash)
